
部署到杭州办公测试环境:
  script:
    - npm install 
    - npm run docs
    - rm -rf /opt/hekr-component/*
    - cp -R docs/* /opt/hekr-component
  tags:
    - hz-pvos
  only:
    - master
  when: always
  environment:
    name: 10.1.1.6
    url: http://10.1.1.6:3200/
